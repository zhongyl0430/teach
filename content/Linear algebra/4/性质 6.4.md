
> [!proposition] 性质 6.4. 
> 设 $\gamma$ 是 ${AX} = b$ 的解, $\eta$ 是 ${AX} = 0$ 的解,则 $\gamma + \eta$ 是 ${AX} = b$ 的解.

`\begin{proof}`
由 ${A\gamma } = b,{A\eta } = 0$ ,可得 $A\left( {\gamma + \eta }\right) = {A\gamma } + {A\eta } = b + 0 = b$ .
`\end{proof}`
