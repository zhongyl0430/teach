若向量组 (I): ${\mathbf{\alpha }}_{1},{\mathbf{\alpha }}_{2},\cdots,{\mathbf{\alpha }}_{s}$ 和向量组 (II): ${\mathbf{\beta }}_{1},{\mathbf{\beta }}_{2},\cdots,{\mathbf{\beta }}_{t}$ 是 $V$ 中的两个向量组, 且向量组 (I) 的每个向量都可以由向量组 (II) 中的向量线性表出, 则称向量组 (I) 可以由向量组 (II) 线性表出. 
若向量组 (I) 和向量组 (II) 可以互相线性表出, 则称它们等价.