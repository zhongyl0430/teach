设 $\mathbf{A}$ 是数域 $F$ 上的一个 $n$ 阶矩阵, $\lambda$ 是一个数. 如果存在非零列向量 $\mathbf{\alpha }$ (即 $n \times 1$ 矩阵),使得
$$
{A\alpha } = {\lambda \alpha }
$$
就称
- $\lambda$ 是 $\mathbf{A}$ 的一个**特征值**, 
- $\mathbf{\alpha }$ 是 $\mathbf{A}$ 的属于特征值 $\lambda$ 的一个**特征向量**, 简称特征向量.